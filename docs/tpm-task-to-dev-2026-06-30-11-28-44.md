# TPM Task: Implement Phase 2 (Socket.io) & Minor UI Addition

**Date/Time:** 2026-06-30 11:28:44
**Status:** Approved for Development (Execution Phase 2)
**Requested by:** User (Homeowner)
**Assigned to:** Dev Team

## 1. Executive Summary
Kerja bagus untuk Tim Dev yang telah menyelesaikan Phase 1 dan Hotfix RBAC dengan sangat cepat! 
Kita sekarang masuk ke **Phase 2**, yang berfokus pada integrasi komunikasi *Real-Time* menggunakan WebSockets (Socket.io). Selain itu, ada satu penambahan UI minor dari Pemilik Bisnis yang harus diselipkan di fase ini.

## 2. Minor UI Addition (Copyright)
- [ ] Di bagian paling bawah (footer) aplikasi frontend, tambahkan teks Copyright sederhana: `© 2026 Brodev. All rights reserved.` 
- [ ] Pastikan teks ini berukuran kecil, tidak mencolok, dan tidak mengganggu area *drag-and-drop* pada Kanban board.

## 3. Backend Requirements (Socket.io)
- [ ] *Install* library `socket.io` di backend.
- [ ] Integrasikan `socket.io` dengan server Express.
- [ ] Pada endpoint `POST /tasks` (Tambah Tugas), `PUT /tasks/:id` (Ubah Status/Jam), dan `DELETE /tasks/:id` (Hapus Tugas):
  - Setelah perubahan tersimpan di database PostgreSQL, *emit* sebuah event (misal: `io.emit('taskUpdated')`) ke seluruh klien yang terhubung agar mereka tahu ada data baru.

## 4. Frontend Requirements (Socket.io-client)
- [ ] *Install* library `socket.io-client` di frontend React.
- [ ] Buat koneksi WebSocket ke *server backend* saat komponen Board di-mount.
- [ ] Dengarkan (*listen*) event `taskUpdated`. Jika event ini diterima:
  - Lakukan *refetch* (pengambilan ulang) data tugas dari API backend, ATAU *update state* React secara lokal.
  - *Goal:* Ketika Bapak menambah/memindah tugas di kantor, layar HP ART di rumah seketika bergeser/ter-update tanpa perlu ditekan *refresh*.
- *(Catatan: Notifikasi/Toast untuk penambahan jam akan difokuskan di Phase 3. Fokus Phase 2 murni pada sinkronisasi posisi & data kartu).*

## 5. Catatan TPM
Silakan eksekusi Phase 2. Karena *infrastructure* dasar sudah kuat dari Phase 1, penambahan Socket.io ini harusnya bisa dilakukan dengan cepat. Beritahu saya (update dokumen ini) jika Phase 2 sudah sukses dites secara *real-time* dengan membuka dua *browser* berdampingan!
