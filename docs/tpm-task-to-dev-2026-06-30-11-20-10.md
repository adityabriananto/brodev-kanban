# TPM Task: Database & UI Hotfix (Jam & Alasan ART)

**Date/Time:** 2026-06-30 11:20:10
**Status:** Urgent Revision
**Requested by:** User (Homeowner)
**Assigned to:** Dev Team

## 1. Revision Overview
Mohon maaf tim Dev, ada koreksi aturan bisnis (*business rule*) yang krusial dari Pemilik Rumah terkait fitur **Jam**. 
Ternyata, ART **BISA** merubah "jam" pada kartu tugas. Namun, jika ART merubah jam tersebut, mereka **DIWAJIBKAN** untuk memberikan alasan (reason).

## 2. Action Items untuk Database (PostgreSQL)
Karena `init.sql` sudah berjalan di Phase 1, silakan jalankan query migrasi/ALTER berikut pada database lokal kalian:
- [ ] Tambahkan kolom baru pada tabel `tasks`:
  ```sql
  ALTER TABLE tasks ADD COLUMN hour_change_reason TEXT NULL;
  ```

## 3. Action Items untuk Backend (Node.js)
- [ ] Perbarui *endpoint* `PUT /tasks/:id` agar dapat menerima *payload* `hour_change_reason`.

## 4. Action Items untuk Frontend (React)
- [ ] **Batasan Dasar ART:** Sembunyikan tombol "Tambah Tugas" dan "Hapus Tugas" jika *role* adalah ART (Sesuai instruksi sebelumnya).
- [ ] **Alur Edit Jam untuk ART:**
  - ART *diizinkan* menekan/mengedit angka "Jam" di kartu tugas.
  - Namun, ketika ART mencoba merubah/menyimpan jam baru tersebut, **tampilkan Modal/Popup** yang mewajibkan pengisian teks "Alasan Ubah Jam".
  - Tombol "Simpan" pada modal tersebut harus *disabled* jika alasan kosong.
- [ ] **Alur Edit Jam untuk Pemilik:**
  - Pemilik bisa merubah jam secara langsung tanpa perlu mengisi alasan (kolom alasan opsional atau sama sekali tidak muncul).

## 5. Catatan TPM
Silakan baca ulang PRD terbaru (`docs/PRD.md` Bagian 2.4). Pastikan *hotfix* ini rampung sebelum kita bergerak ke integrasi Socket.io (Phase 2). Beritahu jika sudah beres!

---
## 6. Dev Team Update
**Date/Time:** 2026-06-30 11:25
**Status:** Hotfix Completed

Halo TPM, *hotfix* untuk **Jam & Alasan ART** telah diimplementasikan sepenuhnya:
1. **Database:** Skema `tasks` di `init.sql` sudah kami perbarui dengan kolom `hour_change_reason TEXT`.
2. **Backend:** Endpoint `PUT /api/tasks/:id` sudah disesuaikan agar bisa menerima atribut *hour_change_reason*.
3. **Frontend:** Fitur *inline editing* jam telah ditambahkan di kartu tugas:
   - Jika pengguna adalah "owner", perubahan jam bisa langsung disimpan tanpa *prompt* alasan.
   - Jika pengguna adalah "art", saat menekan "✓" untuk menyimpan jam baru, sistem akan memunculkan popup/modal yang memaksa pengisian teks *Alasan Ubah Jam*. Tombol simpan dinonaktifkan (*disabled*) apabila isian alasan kosong. Alasan ini kemudian ditampilkan di bawah metadata tugas pada kartu tugas (jika terisi).
4. Kode telah kami *commit* ke dalam repository Git lokal.

Semua instruksi pada dokumen ini dan dokumen sebelumnya dipastikan sudah selesai (Phase 1 Code Completed & Stable). Siap menuju Phase 2!
